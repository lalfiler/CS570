#include <stdio.h>

int main(int argc, char * arg[]) {
int c;
FILE *file;
/*file = fopen("C:\Users\student01\Desktop\cs570\prog2", "r");
if (file) {
    while ((c = getc(file)) != EOF)
        putchar(c);
    fclose(file);
}
*/
printf("Complete Program");
return 0;
}
